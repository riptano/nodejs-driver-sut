'use strict';

// Returns an object containing configuration properties that will be
// merged to the fixed options (contactPoints, localDataCenter)
module.exports = function (driver) {
  return {};
};